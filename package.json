{
  "name": "react-native-model",
  "version": "1.0.3",
  "description": "model to handle validations , sqlite , multi language and etc.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react-native-sqlite-2": "^2.0.1"
  },
  "keywords": [
    "react-native-model",
    "react-native-multi-language",
    "react-native-sqlite"
  ],
  "author": "ziaadini",
  "license": "MIT",
  "devDependencies": {
    "metro-react-native-babel-preset": "^0.54.0"
  }
}
